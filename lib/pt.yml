pt:
  remarkable:
    aasm:
      aasm:
        description: "agir como aasm com o atributo {{name}}"
        expectations:
          aasm_column: "estivesse configurado como aasm com o atributo {{name}}"
          aasm_initial_state: "estivesse que o estado inicial fosse {{initial_state}}, obtive {{actual}}"
          aasm_states: "possuísse os estados {{states}}, possui {{actual}}"
          aasm_events: "possuísse os eventos {{events}}, possui {{actual}}"

